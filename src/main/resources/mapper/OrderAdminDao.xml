<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.Lql.SRTP.dao.OrderAdiminDao">
    <insert id="createoid" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO order
    </insert>
    <insert id="addorderitem">
        INSERT INTO order_item
        (oid, pid, pname, pimage, pprice, pnum, ptotalprice, pcreatetime, pmodifiedtime, pcreateruser, pmodifieduser)
        VALUES (#{oid}, #{pid}, #{pname}, #{pimage}, #{pprice}, #{pnum}, #{ptotalprice}, #{pcreatetime},
                #{pmodifiedtime}, #{pcreateruser}, #{pmodifieduser})
    </insert>
    <select id="readorderitembyoid" resultType="com.Lql.SRTP.entity.Orderitem">
        SELECT *
        FROM srtp_orderitem
        WHERE oid = #{oid}
    </select>
    <insert id="addorder">
        INSERT INTO order
        (oid, order_price, order_createtime, order_reviewer, order_sort, order_note)
        VALUES (#{oid}, #{order_price}, #{order_createtime}, #{order_reviewer}, #{order_sort}, #{order_note})
    </insert>
    <select id="getproductByPid" resultType="com.Lql.SRTP.entity.Product">
        SELECT *
        FROM product
        WHERE id = #{pid}
    </select>

</mapper>
